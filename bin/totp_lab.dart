/*
import 'package:otp/otp.dart';

void main() {
  var secret = "YourSecretKey"; // This should be a base32 encoded string.
  var now = DateTime.now().millisecondsSinceEpoch;
  var code = OTP.generateTOTPCodeString(secret, now, interval: 30, algorithm: Algorithm.SHA1);
  print("TOTP token: $code");
}
*/
